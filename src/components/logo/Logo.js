import React, { Component } from 'react';
import './Logo.scss';

class Logo extends Component {
    render() {
        return (
            <header className="logo">
                its the future app logo!!!
            </header>
        )
    }
}

export default Logo;