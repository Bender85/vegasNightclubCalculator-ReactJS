import React, { Component } from 'react';
import './Compare.scss';

class Compare extends Component {
    render() {
        return(
            <div>This should be compare section</div>
        )
    }
}

export default Compare;